services:
  postgres:
    image: postgres:17-alpine
    container_name: codetest-lab-postgres
    environment:
      POSTGRES_DB: codetestlab
      POSTGRES_USER: codetest
      POSTGRES_PASSWORD: codetest
    ports:
      - "5432:5432"
    volumes:
      - codetest_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U codetest -d codetestlab"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  codetest_pgdata:
