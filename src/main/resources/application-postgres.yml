spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/codetestlab}
    username: ${DB_USER:codetest}
    password: ${DB_PASSWORD:codetest}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
